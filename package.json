{
  "name": "firestorm-iron-column",
  "version": "8.5.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/pack-portable.mjs && node scripts/verify-build.mjs",
    "test": "node --import ./tests/register-asset-loader.mjs --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.0.5",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
